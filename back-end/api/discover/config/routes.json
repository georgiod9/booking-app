{
  "routes": [
    {
      "method": "GET",
      "path": "/discovers",
      "handler": "discover.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/discovers/count",
      "handler": "discover.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/discovers/:id",
      "handler": "discover.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/discovers",
      "handler": "discover.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/discovers/:id",
      "handler": "discover.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/discovers/:id",
      "handler": "discover.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
